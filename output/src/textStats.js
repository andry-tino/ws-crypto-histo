/**
 * textStats.js
 */

function calculateTextStats(text) {
    /**
     * When you write some text in the tool, CryptoHisto will call this function 
     * and will pass the text in the argument. 
     * It is your job to code this function and make it return the frequencies for each letter 
     * so that CryptoHisto can display the chart. For now we are returning all 0 frequencies!
     */

    return data_lang_nil;
}